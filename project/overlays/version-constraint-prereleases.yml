#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"kind": "Trait"}), expects="1+"
---
spec:
  carvelPackages:
  #@overlay/match by=overlay.all, expects="1+"
  - versionSelection:
      #@ if/end data.values.prereleases:
      #@overlay/match missing_ok=True
      prereleases: {}
